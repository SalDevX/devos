efiSystemPartition:       "/boot"
efiSystemPartitionSize:   "512MiB"
efiSystemPartitionName:   "EFI"
# `suspend` (RAM-sized swap for hibernation) is omitted: hibernation is unwired
# on DevOS (no `resume` initramfs hook, no default swap) and is a foot-gun on a
# LUKS install. `small`/`file` remain for ordinary memory-pressure swap.
userSwapChoices:
    - none
    - small
    - file
drawNestedPartitions:     false
alwaysShowPartitionLabels: true
defaultFileSystemType:    "ext4"
availableFileSystemTypes: [ "ext4", "btrfs", "xfs" ]

initialPartitioningChoice: erase
initialSwapChoice:         none
